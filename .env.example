# =============================================================================
# DEPRECATED — this file describes the legacy Bun gateway (now under legacy/).
# For the live AI Gateway MVP, use:
#   deploy/compose/.env.example   — full Docker stack (recommended)
#   deploy/local/.env.example     — host-native dev (IDE debugging)
# See deploy/compose/README.md and deploy/local/README.md
# =============================================================================

# Legacy Bun app only (legacy/docker-compose.yml) — not the MVP stack
